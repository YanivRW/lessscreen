package com.yanivrw.lessscreen

// Replace these with values from your Supabase project:
// Project Settings → API → Project URL and "anon public" key.
// These two values are safe to embed in the client.
object SupabaseConfig {
    const val URL: String = "REPLACE_ME_SUPABASE_URL"
    const val ANON_KEY: String = "REPLACE_ME_SUPABASE_ANON_KEY"
}
